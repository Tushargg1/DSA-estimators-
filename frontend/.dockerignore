# Dependencies — reinstalled with `npm ci` inside the image (task 12.2).
node_modules/

# Build output — regenerated by `npm run build` inside the image.
dist/

# Local env files (secrets / machine-specific). .env.example stays as docs.
.env
.env.*
!.env.example

# VCS
.git/
.gitignore

# IDE / editor cruft
.idea/
.vscode/
*.log

# Docker files themselves
Dockerfile
.dockerignore
nginx.conf

# OS files
.DS_Store
Thumbs.db
